/**
 * Defines the different sizes of vehicles the garage can handle.
 * Using an enum makes the code type-safe and readable.
 */
package Advanced_parkingLot;
public enum VehicleType {
    SMALL,
    MEDIUM,
    LARGE
}