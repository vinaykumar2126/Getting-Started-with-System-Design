// Represents a single, resource-intensive database connection.
package DatabaseConnection;

public class DatabaseConnection {
    public void executeQuery(String query) {
        System.out.println("Executing query: " + query);
    }
}